extends layouts/default.pug
include mixins/box-head.pug

block main 
      +box-head("trang san pham","day la doan mo ta san pham") 

      ul 
            each item in products 
                  li #{item.tiltle} #{item.price} #{item.thumnail}